#Compute Maximum of Numbers
#A Python program that computes the maximum of two inputs:

#The first input should be the absolute of -4.
#The second input should be the length of the string "abc".


first = abs(-4)
second = len("abc")

maximum_value = max(first, second)

print(maximum_value)